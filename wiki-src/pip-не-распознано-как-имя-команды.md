# [← Python](Python)

# pip не распознано как имя команды

## Симптомы
При выполнении команды `pip install pillow` или любого другого вызова команды `pip`

Windows выводит сообщение:
`"pip не распознано как имя функции, команды или командлета"`

## Причина

Python был установлен без добавления в системную переменную PATH
(не отмечена галочка Add Python to PATH во время установки).

Из-за этого:

- команда `pip` недоступна напрямую

- Python при этом может запускаться

## Временное решение (быстрое)

Если Python запускается, выполните одну из команд:

```bash
python -m pip install pillow
```

или

```bash
py -m pip install pillow
```

📌 Команду нужно выполнять в той среде, где Python корректно распознаётся.

## Основное решение (рекомендуемое)

- Удалить текущую установку Python

- Скачать Python заново по инструкции ниже

- Обязательно отметить галочку:

☑ Add Python to PATH

- Завершить установку

После этого можно использовать стандартную команду:

`pip install <название модуля>`

## Рекомендации для учебных классов

- Проверять наличие Python в PATH сразу после установки

- Использовать одинаковую версию Python на всех ПК

- При первичной настройке — проверять `pip --version`

## Полезные материалы:
- [PYTHON инструкция по установке.pdf](https://github.com/user-attachments/files/25524041/PYTHON.pdf)
- [Инструкция по установке IDE.pdf](https://github.com/user-attachments/files/25524072/IDE.pdf)

💬 Обсуждение проблемы: https://github.com/ErnestMatskevich/kb-knowledge/issues/2
